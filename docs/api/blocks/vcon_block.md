# VCONBlock

Wraps any module in an (original, compressed) pair and blends their outputs
with a scalar `beta`. Used for smooth knowledge-distillation-style fine-tuning.

See [Concepts](../../concepts.md) for the full workflow.

::: transformersurgeon.blocks.vcon_block.VCONBlock
