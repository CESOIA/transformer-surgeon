# CompressionSchemesManager

The main user-facing entry point. Holds all `CompressionScheme` objects for a
model, provides layer filtering via `criteria`, and orchestrates calibration
and compression application.

::: transformersurgeon.utils.manager.CompressionSchemesManager
