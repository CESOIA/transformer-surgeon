# CompressionScheme

Binds one model path (e.g., `model.layers.3.self_attn.q_proj`) to one set of
compressor configs. Created automatically by the manager from model indexing
metadata — you do not instantiate this directly.

::: transformersurgeon.utils.scheme.CompressionScheme
